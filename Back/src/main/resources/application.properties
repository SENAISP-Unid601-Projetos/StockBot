spring.application.name=Back

# --- Conexão Forçada (Sem variáveis de ambiente) ---
# Usamos 127.0.0.1 para garantir que vai via IPv4
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/stockbot?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456
# Forçamos o driver para garantir que ele não tenta usar o do PostgreSQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Token Secret
api.security.token.secret=Opala85ComVidroFumeSuperSecretoNinguemDescobre123!