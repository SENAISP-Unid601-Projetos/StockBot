spring.application.name=Back

# Configuracao do banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/stockbot
spring.datasource.username=root
spring.datasource.password=aluno
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuracao do JPA (Hibernate)
# AVISO: Em producao, NUNCA use 'update' ou 'create'.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# A linha abaixo foi corrigida para nao incluir o comentario
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT SECRET
api.security.token.secret=Opala85ComVidroFumeSuperSecretoNinguemDescobre123!