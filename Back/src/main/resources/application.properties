spring.application.name=Back

# --- Configuracao Hibrida ---

# Usa variaveis de ambiente (padrao do Render/Docker).
# Se nao existirem (uso local), usa os valores apos os dois pontos.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/stockbot}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:aluno}

# Configuracao do JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT SECRET
api.security.token.secret=Opala85ComVidroFumeSuperSecretoNinguemDescobre123!